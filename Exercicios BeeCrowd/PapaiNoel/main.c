#include <stdio.h>
#include <stdio.lib>

int main{
    int ponto;
    char str[100];
    char[0] = ATR
    str
    scanf("%4s",str );
    scanf("[^\n]",str);
    while(str != "0"){
        if(){
            char direçao
        }
    }
    scanf("%d", &ponto);
    
}